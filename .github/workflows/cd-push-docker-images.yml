name: Build & publish spinoza images
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - folde: ./backend/Managers/CatalogManager/
            image: spinozaacr.azurecr.io/spinoza.backend.managers.catalogmanager
          - folder: ./backend/Accessors/TestCatalogAccessor/
            image: spinozaacr.azurecr.io/spinoza.backend.accessors.testaccessor
          - folder: ./backend/Accessors/QuestionCatalogAccessor/
            image: spinozaacr.azurecr.io/spinoza.backend.accessors.qustionaccessor
          - folder: ./backend/Accessors/TagCatalogAccessor/
            image: spinozaacr.azurecr.io/spinoza.backend.accessors.tagaccessor
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
          login-server: spinozaacr.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: ${{ matrix.image }}

      - name: Build and push Docker images
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ${{ matrix.folder }}
          file: ${{ matrix.folder }}Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
